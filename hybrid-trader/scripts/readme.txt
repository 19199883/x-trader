configurator.py needs to be placed tools subdirectory of trader.
configurator.py updates travs.config using stra_settings.csv file.

todos:
1. support the feature: configurator.py checks whether strategy so files(which are configured in trasev.config) exist under trader root directory
2. support the feature: configurator.py update ev files and check whether they exist.
